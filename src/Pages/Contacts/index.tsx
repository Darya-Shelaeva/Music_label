import React from "react";
import Header from "../../components/Header";

const Contacts = () => {
    return(
        <div>
            <Header></Header>
        </div>

    )
}

export default Contacts;